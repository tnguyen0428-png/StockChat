# Dependencies
node_modules/

# Environment variables — never commit real secrets
.env
.env.local
.env.*.local

# Build output
dist/
dist-*/

# Vite temp files
vite.config.js.timestamp-*

# Supabase CLI scratch (linked-project, version caches, etc.)
supabase/.temp/

# Claude Code session data
.claude/

# Scratch / diagnostic scripts (one-off debugging, not prod code)
check_recording.mjs
scripts/diagnose-*
scripts/query-*
scripts/wipe-*

# Mockups (design artifacts, not runtime)
mockup-*.html

# OS
.DS_Store
Thumbs.db
